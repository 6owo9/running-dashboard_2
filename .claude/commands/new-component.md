# 새 컴포넌트 명령

새 React 컴포넌트를 만들 때 사용한다.

1. 기존 `frontend/src/components/` 구조를 먼저 확인한다.
2. 재사용 가능한 UI인지 페이지 전용 UI인지 판단한다.
3. 파일명은 `PascalCase.tsx`로 만든다.
4. props 타입은 컴포넌트 파일 안에 명확히 둔다.
5. 버튼에는 가능한 경우 `type="button"`을 지정한다.
6. 모바일 폭에서 텍스트와 버튼이 넘치지 않는지 확인한다.
