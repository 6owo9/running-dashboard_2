# Codex 리뷰어 메모리

- 이 프로젝트는 단일 메인 페이지 중심 구조입니다.
- 프론트 API 타입과 백엔드 DTO 필드명이 어긋나면 화면이 조용히 깨질 수 있습니다.
- 지도 기능은 Leaflet 레이어 정리 누락으로 중복 마커가 생기기 쉽습니다.
- `application-local.yml`의 `8081` 포트와 Vite proxy `8080` 불일치를 항상 확인합니다.
- CCTV 기능은 `ITS_API_KEY`가 없을 때도 앱 전체가 깨지면 안 됩니다.
